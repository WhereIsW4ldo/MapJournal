import {StyleSheet} from "react-native";

const styles = StyleSheet.create({
   map: {
       position: "absolute",
       width: "100%",
       height: "100%",
       left: 0,
       top: 0,
   } 
});

export default styles;